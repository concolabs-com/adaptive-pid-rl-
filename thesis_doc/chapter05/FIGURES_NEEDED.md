# Chapter 5 — Figures

Results already carry **12 embedded figures** (Fig 5.0–5.10, generated by
`make_all_figures.py`, plus the two anti-windup trajectory panels). This file
tracks the **remaining candidates** and one **blocked** figure. New data
figures belong in `make_all_figures.py` (reads `benchmark_results/` CSVs).

| Fig | Title | Status | §/where | Why it helps |
|-----|-------|--------|---------|--------------|
| — | IAE & success-rate bars (companion to the settling bars) | 🔶 generatable | §5.3 | Settling is only one metric; IAE + Wilson-interval success bars from `aggregate_all_seeds.csv` complete the RQ2 picture |
| — | Per-seed training-curve spread (mean ± band over 5 seeds) | 🔶 generatable | §5.1 | Fig 5.0 is seed 7 only; a 5-seed band substantiates "qualitatively identical across seeds" |
| — | GRU vs frame-stack vs context settling bars (RQ3b) | 🔶 generatable | §5.4.2 | Table 5.3 has the numbers; a bar chart makes the recurrence advantage visible (flag the capacity confound, L4) |
| — | Adaptation-lag bars (re-inference time from the mass shock) | 🔶 generatable | §5.8 | `gain_analysis/adaptation_lag.csv` — quantifies the ~2.8 s student re-inference cost |
| 5.x | **Mid-approach shock trajectories** (mass ×2.5, actuator ×0.5) | ⚠️ blocked | §5.8 (placeholder "Figure 5.x" at line ~448) | **Do not embed as-is.** The raw `stage6_shock_probe/*.png` are single rollouts: the actuator panel shows a *success* (contradicts Table 5.5's 30%/timeout), and the mass-shock Kd traces read inverted vs the §5.8 text ("context steps up promptly, blind gradual"). Reconcile the §5.8 prose with the data, or regenerate a representative/aggregate plot, then embed and renumber 5.8→5.10 downstream |

**Notes**
- The single remaining `Figure 5.x` placeholder in `chapter5_results.md` (§5.8,
  the shock paragraph) is intentional — it marks the blocked figure above.
- Highest-value next addition for a strict committee: the **IAE/success bars**
  (every quantitative claim then has a figure) and the **GRU bar** (gives RQ3b
  a visual). Both are low-risk CSV plots.
- Adding any figure *before* §5.8 shifts the 5.x numbering — update the captions
  and the three inline cross-refs (Figs 5.2, 5.4, 5.9) accordingly.
